module Kwakwala.GUI.Widgets.MaybeWidget
  (
  ) where

import Control.Lens

import Data.Text qualified as T
import Data.Text (Text)

import Monomer

{-
maybeWidgetV ::
   ( WidgetModel s
   , WidgetEvent e
   ) => ALens' s (Maybe a)
   -> Text
   -> WidgetNode s e
   -> WidgetNode s e
maybeWidgetV txt wgt
-}

